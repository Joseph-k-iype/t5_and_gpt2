def merge_process_cells_in_table(table):
    """
    Merge cells in the first column ("Process") for identical process names in the PowerPoint table.
    Parameters:
    - table: The PowerPoint table object.
    """
    process_column_idx = 0  # Index of the "Process" column in the table
    merge_start = None  # Start row for merging
    previous_value = None
    total_rows = len(table.rows)  # Total rows in the PowerPoint table

    for row_idx in range(1, total_rows):  # Start at row 1 to skip the header
        current_value = table.cell(row_idx, process_column_idx).text.strip()  # Get the current cell text

        if current_value == previous_value:
            # Continue the merge group
            if merge_start is None:
                merge_start = row_idx - 1  # Mark the start of the merge
        else:
            # Finalize the previous merge
            if merge_start is not None and merge_start < row_idx - 1:
                table.cell(merge_start, process_column_idx).merge(table.cell(row_idx - 1, process_column_idx))
                merge_start = None  # Reset the merge start

        previous_value = current_value

    # Finalize the last merge group
    if merge_start is not None and merge_start < total_rows - 1:
        table.cell(merge_start, process_column_idx).merge(table.cell(total_rows - 1, process_column_idx))
