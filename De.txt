import React from 'react';
import { RotateCcw, ZoomIn, ZoomOut, Maximize2 } from 'lucide-react';
import { Country, Stats } from '@/types/lineage';

interface ControlPanelProps {
  countries: Country[];
  stats: Stats | null;
  selectedCountry: string;
  onCountryChange: (country: string) => void;
  onReset: () => void;
  onZoomIn: () => void;
  onZoomOut: () => void;
  onFitView: () => void;
}

const ControlPanel: React.FC<ControlPanelProps> = ({
  countries,
  stats,
  selectedCountry,
  onCountryChange,
  onReset,
  onZoomIn,
  onZoomOut,
  onFitView,
}) => {
  return (
    <div
      style={{
        position: 'absolute',
        top: '20px',
        left: '20px',
        zIndex: 10,
        background: 'white',
        borderRadius: '12px',
        padding: '16px',
        boxShadow: '0 4px 12px rgba(0, 0, 0, 0.15)',
        minWidth: '280px',
      }}
    >
      <h2
        style={{
          fontSize: '18px',
          fontWeight: 700,
          marginBottom: '16px',
          color: '#1e293b',
        }}
      >
        Data Lineage Control
      </h2>

      {/* Stats */}
      {stats && (
        <div
          style={{
            display: 'grid',
            gridTemplateColumns: '1fr 1fr',
            gap: '8px',
            marginBottom: '16px',
          }}
        >
          <div
            style={{
              background: '#f1f5f9',
              padding: '8px',
              borderRadius: '6px',
            }}
          >
            <div style={{ fontSize: '20px', fontWeight: 700, color: '#3b82f6' }}>
              {stats.total_countries}
            </div>
            <div style={{ fontSize: '11px', color: '#64748b' }}>Countries</div>
          </div>
          <div
            style={{
              background: '#f1f5f9',
              padding: '8px',
              borderRadius: '6px',
            }}
          >
            <div style={{ fontSize: '20px', fontWeight: 700, color: '#8b5cf6' }}>
              {stats.total_databases}
            </div>
            <div style={{ fontSize: '11px', color: '#64748b' }}>Databases</div>
          </div>
          <div
            style={{
              background: '#f1f5f9',
              padding: '8px',
              borderRadius: '6px',
            }}
          >
            <div style={{ fontSize: '20px', fontWeight: 700, color: '#ec4899' }}>
              {stats.total_attributes}
            </div>
            <div style={{ fontSize: '11px', color: '#64748b' }}>Attributes</div>
          </div>
          <div
            style={{
              background: '#f1f5f9',
              padding: '8px',
              borderRadius: '6px',
            }}
          >
            <div style={{ fontSize: '20px', fontWeight: 700, color: '#10b981' }}>
              {stats.total_transfers}
            </div>
            <div style={{ fontSize: '11px', color: '#64748b' }}>Transfers</div>
          </div>
        </div>
      )}

      {/* Country Filter */}
      <div style={{ marginBottom: '16px' }}>
        <label
          style={{
            display: 'block',
            fontSize: '12px',
            fontWeight: 600,
            color: '#64748b',
            marginBottom: '6px',
          }}
        >
          Filter by Country
        </label>
        <select
          value={selectedCountry}
          onChange={(e) => onCountryChange(e.target.value)}
          style={{
            width: '100%',
            padding: '8px 12px',
            borderRadius: '6px',
            border: '1px solid #e2e8f0',
            fontSize: '14px',
            color: '#1e293b',
            background: 'white',
            cursor: 'pointer',
          }}
        >
          <option value="">All Countries</option>
          {countries.map((country) => (
            <option key={country.code} value={country.code}>
              {country.name}
            </option>
          ))}
        </select>
      </div>

      {/* Controls */}
      <div
        style={{
          display: 'flex',
          gap: '8px',
          marginBottom: '12px',
        }}
      >
        <button
          onClick={onZoomIn}
          style={{
            flex: 1,
            padding: '8px',
            borderRadius: '6px',
            border: '1px solid #e2e8f0',
            background: 'white',
            cursor: 'pointer',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: '4px',
            fontSize: '13px',
            fontWeight: 500,
            color: '#64748b',
          }}
          title="Zoom In"
        >
          <ZoomIn size={16} />
        </button>
        <button
          onClick={onZoomOut}
          style={{
            flex: 1,
            padding: '8px',
            borderRadius: '6px',
            border: '1px solid #e2e8f0',
            background: 'white',
            cursor: 'pointer',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: '4px',
            fontSize: '13px',
            fontWeight: 500,
            color: '#64748b',
          }}
          title="Zoom Out"
        >
          <ZoomOut size={16} />
        </button>
        <button
          onClick={onFitView}
          style={{
            flex: 1,
            padding: '8px',
            borderRadius: '6px',
            border: '1px solid #e2e8f0',
            background: 'white',
            cursor: 'pointer',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: '4px',
            fontSize: '13px',
            fontWeight: 500,
            color: '#64748b',
          }}
          title="Fit View"
        >
          <Maximize2 size={16} />
        </button>
      </div>

      <button
        onClick={onReset}
        style={{
          width: '100%',
          padding: '10px',
          borderRadius: '6px',
          border: 'none',
          background: '#3b82f6',
          color: 'white',
          cursor: 'pointer',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          gap: '6px',
          fontSize: '14px',
          fontWeight: 600,
        }}
      >
        <RotateCcw size={16} />
        Reset View
      </button>

      {/* Instructions */}
      <div
        style={{
          marginTop: '16px',
          padding: '12px',
          background: '#f8fafc',
          borderRadius: '6px',
          fontSize: '11px',
          color: '#64748b',
          lineHeight: '1.5',
        }}
      >
        <strong>Instructions:</strong>
        <ul style={{ margin: '6px 0 0 16px', padding: 0 }}>
          <li>Click nodes to highlight lineage</li>
          <li>Click chevrons to expand/collapse</li>
          <li>Drag to pan, scroll to zoom</li>
        </ul>
      </div>
    </div>
  );
};

export default ControlPanel;
