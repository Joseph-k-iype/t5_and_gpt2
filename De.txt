/* frontend/src/index.css */
@import 'tailwindcss/base';
@import 'tailwindcss/components';
@import 'tailwindcss/utilities';

/* Import Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Crimson+Text:ital,wght@0,400;0,600;1,400;1,600&family=Inter:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;0,700;1,400;1,700&family=JetBrains+Mono:wght@300;400;500&display=swap');

/* Global styles with New Yorker inspiration */
:root {
  --font-serif: 'Crimson Text', 'Times New Roman', serif;
  --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
  --font-display: 'Playfair Display', Georgia, serif;
  --font-mono: 'JetBrains Mono', Menlo, Monaco, monospace;
  
  /* New Yorker color palette */
  --color-primary: #1a1a1a;
  --color-secondary: #2d2d2d;
  --color-accent: #c41e3a;
  --color-background: #fffef7;
  --color-surface: #fefefe;
  --color-muted: #6b7280;
  --color-border: #e5e5e5;
  
  /* Spacing inspired by print layouts */
  --spacing-base: 1rem;
  --spacing-comfortable: 1.5rem;
  --spacing-generous: 3rem;
  
  /* Typography scale */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;
  --text-4xl: 2.25rem;
}

/* Base typography */
body {
  font-family: var(--font-serif);
  color: var(--color-primary);
  background-color: var(--color-background);
  line-height: 1.6;
  font-size: var(--text-base);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Headings with elegant typography */
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-display);
  font-weight: 400;
  line-height: 1.2;
  margin-bottom: 1rem;
  color: var(--color-primary);
}

h1 {
  font-size: var(--text-4xl);
  letter-spacing: -0.025em;
}

h2 {
  font-size: var(--text-3xl);
  letter-spacing: -0.025em;
}

h3 {
  font-size: var(--text-2xl);
}

h4 {
  font-size: var(--text-xl);
  font-weight: 600;
}

/* Paragraph styling with generous line height */
p {
  margin-bottom: 1.5rem;
  line-height: 1.75;
}

/* Links with subtle underlines */
a {
  color: var(--color-primary);
  text-decoration: underline;
  text-decoration-thickness: 1px;
  text-underline-offset: 3px;
  transition: all 0.2s ease;
}

a:hover {
  color: var(--color-accent);
  text-decoration-thickness: 2px;
}

/* Button styling inspired by print design */
.btn {
  font-family: var(--font-sans);
  font-weight: 500;
  padding: 0.75rem 2rem;
  border: 1px solid var(--color-border);
  background: var(--color-surface);
  color: var(--color-primary);
  text-decoration: none;
  transition: all 0.2s ease;
  cursor: pointer;
  display: inline-block;
  text-align: center;
}

.btn:hover {
  background: var(--color-primary);
  color: var(--color-surface);
  border-color: var(--color-primary);
}

.btn-primary {
  background: var(--color-primary);
  color: var(--color-surface);
  border-color: var(--color-primary);
}

.btn-primary:hover {
  background: var(--color-accent);
  border-color: var(--color-accent);
}

/* Input styling */
.input {
  font-family: var(--font-serif);
  padding: 0.75rem 1rem;
  border: 1px solid var(--color-border);
  background: var(--color-surface);
  color: var(--color-primary);
  font-size: var(--text-base);
  line-height: 1.5;
  transition: all 0.2s ease;
  width: 100%;
}

.input:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgba(26, 26, 26, 0.1);
}

/* Card styling with subtle shadows */
.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  padding: var(--spacing-comfortable);
  transition: all 0.2s ease;
}

.card:hover {
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  transform: translateY(-2px);
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: var(--color-background);
}

::-webkit-scrollbar-thumb {
  background: var(--color-border);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--color-muted);
}

/* Selection styling */
::selection {
  background: var(--color-accent);
  color: white;
}

/* frontend/src/styles/newyorker-theme.css */
/* New Yorker Magazine Inspired Theme */

/* Layout containers */
.magazine-layout {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
  background: var(--color-background);
}

.article-container {
  max-width: 65ch;
  margin: 0 auto;
  padding: var(--spacing-generous) 0;
}

/* Typography scale inspired by The New Yorker */
.headline {
  font-family: var(--font-display);
  font-size: clamp(1.5rem, 4vw, 3rem);
  font-weight: 400;
  line-height: 1.1;
  letter-spacing: -0.02em;
  margin-bottom: 1rem;
  color: var(--color-primary);
}

.subheadline {
  font-family: var(--font-serif);
  font-size: clamp(1rem, 2.5vw, 1.5rem);
  font-weight: 400;
  line-height: 1.4;
  margin-bottom: 2rem;
  color: var(--color-muted);
  font-style: italic;
}

.byline {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: 2rem;
  color: var(--color-muted);
}

.body-text {
  font-family: var(--font-serif);
  font-size: var(--text-lg);
  line-height: 1.8;
  margin-bottom: 1.5rem;
  color: var(--color-primary);
}

.drop-cap {
  float: left;
  font-family: var(--font-display);
  font-size: 4rem;
  line-height: 1;
  margin: 0.1rem 0.5rem 0 0;
  color: var(--color-primary);
}

/* Pull quotes and callouts */
.pull-quote {
  font-family: var(--font-display);
  font-size: var(--text-2xl);
  font-style: italic;
  text-align: center;
  margin: 3rem 0;
  padding: 2rem;
  border-top: 1px solid var(--color-border);
  border-bottom: 1px solid var(--color-border);
  color: var(--color-primary);
  position: relative;
}

.pull-quote::before,
.pull-quote::after {
  content: '"';
  font-size: 3rem;
  color: var(--color-muted);
  position: absolute;
}

.pull-quote::before {
  top: 0;
  left: 1rem;
}

.pull-quote::after {
  bottom: 0;
  right: 1rem;
}

/* Sidebar and marginalia */
.sidebar {
  background: var(--color-surface);
  border-left: 3px solid var(--color-accent);
  padding: 2rem;
  margin: 2rem 0;
  font-size: var(--text-sm);
  line-height: 1.6;
}

.marginalia {
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  color: var(--color-muted);
  border-left: 2px solid var(--color-border);
  padding-left: 1rem;
  margin: 1rem 0;
  font-style: italic;
}

/* List styling */
.editorial-list {
  margin: 2rem 0;
  padding-left: 0;
  list-style: none;
}

.editorial-list li {
  margin-bottom: 1rem;
  padding-left: 2rem;
  position: relative;
}

.editorial-list li::before {
  content: '•';
  color: var(--color-accent);
  font-weight: bold;
  position: absolute;
  left: 0;
}

/* Dividers and separators */
.section-divider {
  width: 100px;
  height: 1px;
  background: var(--color-border);
  margin: 3rem auto;
  border: none;
}

.ornamental-divider {
  text-align: center;
  margin: 3rem 0;
  font-size: var(--text-lg);
  color: var(--color-muted);
}

.ornamental-divider::before {
  content: '• • •';
  letter-spacing: 1rem;
}

/* Interactive elements */
.hover-lift {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.hover-lift:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
}

.subtle-animation {
  animation: fadeIn 0.6s ease-out;
}

/* Responsive typography */
@media (max-width: 768px) {
  .article-container {
    padding: 2rem 1rem;
  }
  
  .body-text {
    font-size: var(--text-base);
    line-height: 1.7;
  }
  
  .pull-quote {
    font-size: var(--text-xl);
    padding: 1.5rem;
  }
}

/* Print-inspired spacing and rhythm */
.content-rhythm > * + * {
  margin-top: 1.5rem;
}

.content-rhythm > h2 + *,
.content-rhythm > h3 + * {
  margin-top: 1rem;
}

.content-rhythm > * + h2,
.content-rhythm > * + h3 {
  margin-top: 3rem;
}

/* Elegant focus states */
.focus-elegant:focus {
  outline: none;
  box-shadow: 0 0 0 3px rgba(196, 30, 58, 0.1);
  border-color: var(--color-accent);
}

/* Loading states */
.loading-text {
  background: linear-gradient(90deg, var(--color-muted) 25%, var(--color-border) 50%, var(--color-muted) 75%);
  background-size: 200% 100%;
  animation: loading 1.5s infinite;
  color: transparent;
  border-radius: 2px;
}

@keyframes loading {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}
