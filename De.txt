import React from 'react';
import { NodeProps, Handle, Position } from '@xyflow/react';
import { CountryNodeData } from '@/types/node.types';

// Country flag mapping for common countries
const countryFlags: Record<string, string> = {
  'DE': '🇩🇪',
  'FR': '🇫🇷',
  'IT': '🇮🇹',
  'ES': '🇪🇸',
  'CA': '🇨🇦',
  'CH': '🇨🇭',
  'JP': '🇯🇵',
  'NZ': '🇳🇿',
  'KR': '🇰🇷',
  'GB': '🇬🇧',
  'US': '🇺🇸',
  'AU': '🇦🇺',
  'SE': '🇸🇪',
  'NO': '🇳🇴',
  'DK': '🇩🇰',
  'FI': '🇫🇮',
  'NL': '🇳🇱',
  'BE': '🇧🇪',
  'AT': '🇦🇹',
  'PT': '🇵🇹'
};

const CountryNode: React.FC<NodeProps<CountryNodeData>> = ({ data }) => {
  const typeStyles = {
    applicable: {
      container: 'bg-green-50 border-green-300',
      header: 'bg-green-100 text-green-800',
      badge: 'bg-green-100 text-green-800 border-green-200 hover:bg-green-200',
      icon: '🌍',
      accent: 'bg-green-500'
    },
    adequacy: {
      container: 'bg-emerald-50 border-emerald-300',
      header: 'bg-emerald-100 text-emerald-800',
      badge: 'bg-emerald-100 text-emerald-800 border-emerald-200 hover:bg-emerald-200',
      icon: '✅',
      accent: 'bg-emerald-500'
    }
  };

  const style = typeStyles[data.type] || typeStyles.applicable;

  return (
    <div className={`
      relative p-4 rounded-xl border-2 shadow-lg min-w-[220px] max-w-[300px]
      transition-all duration-200 hover:shadow-xl
      ${style.container}
    `}>
      {/* Connection Handle */}
      <Handle
        type="target"
        position={Position.Top}
        className="w-3 h-3 bg-gray-400 border-2 border-white shadow-md"
      />
      
      {/* Header */}
      <div className={`
        flex items-center gap-2 mb-3 px-3 py-2 rounded-lg border
        ${style.header}
      `}>
        <span className="text-base">{style.icon}</span>
        <h4 className="font-semibold text-sm">{data.label}</h4>
        <span className="ml-auto text-xs font-bold bg-white px-2 py-1 rounded-full">
          {data.countries.length}
        </span>
      </div>
      
      {/* Countries Grid */}
      <div className="flex flex-wrap gap-2">
        {data.countries.map((country, index) => (
          <div
            key={index}
            className={`
              group flex items-center gap-1 px-2 py-1 rounded-lg text-xs font-medium 
              border transition-all duration-150 cursor-help
              ${style.badge}
            `}
            title={`Country Code: ${country}`}
          >
            <span className="text-sm">
              {countryFlags[country] || '🏳️'}
            </span>
            <span className="font-mono font-semibold">
              {country}
            </span>
          </div>
        ))}
      </div>
      
      {/* Footer info */}
      <div className="mt-3 pt-2 border-t border-gray-200">
        <div className="flex items-center justify-between text-xs text-gray-600">
          <span>Total Countries</span>
          <span className="font-bold text-gray-800">{data.countries.length}</span>
        </div>
      </div>
      
      {/* Visual accent */}
      <div className={`
        absolute top-0 left-0 w-1 h-full rounded-l-xl
        ${style.accent}
      `} />
      
      {/* Type indicator */}
      <div className="absolute top-2 right-2">
        <div className={`
          w-2 h-2 rounded-full animate-pulse
          ${style.accent}
        `} />
      </div>
    </div>
  );
};

export default CountryNode;
