import React from 'react';
import { NodeProps, Handle, Position } from '@xyflow/react';
import { RuleNodeData } from '@/types/node.types';

const RuleNode: React.FC<NodeProps<RuleNodeData>> = ({ data, selected }) => {
  const { rule } = data;
  
  const priorityStyles = {
    high: 'bg-red-50 border-red-300 text-red-900',
    medium: 'bg-yellow-50 border-yellow-300 text-yellow-900',
    low: 'bg-green-50 border-green-300 text-green-900'
  };

  const priorityBadgeStyles = {
    high: 'bg-red-100 text-red-800 border-red-200',
    medium: 'bg-yellow-100 text-yellow-800 border-yellow-200',
    low: 'bg-green-100 text-green-800 border-green-200'
  };

  const priorityAccentStyles = {
    high: 'bg-red-500 text-white border-red-600',
    medium: 'bg-yellow-500 text-white border-yellow-600',
    low: 'bg-green-500 text-white border-green-600'
  };

  return (
    <div className={`
      relative p-4 rounded-xl border-2 shadow-lg min-w-[320px] max-w-[420px] 
      transition-all duration-200 cursor-pointer
      ${priorityStyles[rule.priority] || 'bg-gray-50 border-gray-300 text-gray-900'}
      ${selected ? 'ring-2 ring-blue-500 shadow-2xl scale-105' : 'hover:shadow-xl hover:scale-102'}
    `}>
      {/* Connection Handles */}
      <Handle
        type="target"
        position={Position.Top}
        className="w-3 h-3 bg-gray-400 border-2 border-white shadow-md"
      />
      <Handle
        type="source"
        position={Position.Bottom}
        className="w-3 h-3 bg-gray-400 border-2 border-white shadow-md"
      />
      
      {/* Header with badges */}
      <div className="flex items-center justify-between mb-3">
        <span className={`
          px-3 py-1 rounded-lg text-xs font-semibold border
          ${priorityBadgeStyles[rule.priority] || 'bg-gray-100 text-gray-800 border-gray-200'}
        `}>
          {rule.rule_type.replace(/_/g, ' ').toUpperCase()}
        </span>
        <span className={`
          px-3 py-1 rounded-lg text-xs font-bold border shadow-sm
          ${priorityAccentStyles[rule.priority] || 'bg-gray-500 text-white border-gray-600'}
        `}>
          {rule.priority?.toUpperCase()}
        </span>
      </div>
      
      {/* Rule Definition */}
      <h3 className="font-bold text-sm mb-3 leading-tight text-gray-800">
        {rule.rule_definition}
      </h3>
      
      {/* Details */}
      <div className="text-xs space-y-2 text-gray-700">
        <div className="flex items-start">
          <span className="font-semibold mr-2 text-gray-600">Reference:</span>
          <span className="text-blue-600 font-medium">{rule.reference}</span>
        </div>
        <div className="flex items-start">
          <span className="font-semibold mr-2 text-gray-600">Domain:</span>
          <span className="capitalize">{rule.domain.replace(/_/g, ' ')}</span>
        </div>
        
        {/* Stats */}
        <div className="flex items-center gap-4 mt-3 pt-2 border-t border-gray-200">
          <div className="flex items-center gap-1">
            <span className="w-2 h-2 bg-green-500 rounded-full"></span>
            <span className="text-xs text-gray-600">{rule.applicable_countries.length} Countries</span>
          </div>
          <div className="flex items-center gap-1">
            <span className="w-2 h-2 bg-purple-500 rounded-full"></span>
            <span className="text-xs text-gray-600">{rule.conditions.length} Conditions</span>
          </div>
          <div className="flex items-center gap-1">
            <span className="w-2 h-2 bg-orange-500 rounded-full"></span>
            <span className="text-xs text-gray-600">{rule.aggregated_roles.length} Roles</span>
          </div>
        </div>
      </div>
      
      {/* Right-click hint */}
      <div className="mt-3 p-2 bg-gray-100/70 backdrop-blur-sm rounded-lg text-xs italic text-gray-600 text-center border border-gray-200">
        <span className="inline-flex items-center gap-1">
          🖱️ Right-click to explore details
        </span>
      </div>
    </div>
  );
};

export default RuleNode;
